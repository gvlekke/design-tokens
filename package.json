{
  "name": "design-tokens",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "design-tokens": "style-dictionary build --config design-tokens.config.json --platform scss",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "style-dictionary": "^3.7.1"
  }
}